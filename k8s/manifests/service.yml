apiVersion: v1
kind: Service
metadata: 
    name: go-web-app-service
    labels:
        app: go-web-app-service
spec:
    ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
    selector:
        app: go-web-app
    type: ClusterIP